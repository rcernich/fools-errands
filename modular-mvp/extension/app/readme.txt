This project produces the composite WAR, using the configuration from bundle/app-parent.

The application generated is identical to that created by the bundle project, except that it uses the current version of this extension.

To test, update the version in extension/pom.xml to 0.0.2-SNAPSHOT and modify the extension.  The application produced by this project should contain the modified functionality.
